package com.example.menu.menurestaurante;
// make  click listener interface to sent data from fragment(meals) to main activity (to add it in DB)
public interface OnItemClickListener {
    void onItemClick(Meals meals);
}


