package com.example.menu.menurestaurante;

// make  click listener interface to sent data from fragment(drinks) to main activity (to add it in DB)
public interface OnDrinkItemClickListener {
    void onItemClickListener(Drinks drinks);
}
